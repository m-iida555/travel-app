
<div class="around-line">
  <div class="room-index">
    <h3>予約済みルーム一覧</h3>

  
  <table>
  <tr>
    <th class="rooms-font contents-width">ルーム画像</th>
    <th class="rooms-font contents-width">ルーム名</th>
    <th class="rooms-font contents-width">ルーム紹介</th>
    <th class="rooms-font contents-width">合計料金</th>
    <th class="rooms-font contents-width">開始日</th>
    <th class="rooms-font contents-width">終了日</th>
    <th class="rooms-font contents-width">予約確定日</th>
  </tr>

 <% @reservations.each do |room| %>
  <tr>
    <td class="contents-width"><%= image_tag room.roompic,:class=>"img-size"%></td>
    <td class="contents-width"><%= link_to room.roomname, new_reserve_reservation_path(room.room_id) %></td> <!-- link_toのパスがshowの時、controllerのshowアクションでparamsからidを拾っていれば、link_toでid指定はいらない。roomだけ　<%#=link_to "編集する",edit_blog_path(blog.id) %>）-->
    <td class="contents-width"><%= room.roominfo %></td>
    <td class="contents-width"><%= room.total_money %></td>
    <td class="contents-width"><%= room.start %></td>
    <td class="contents-width"><%= room.end %></td>
    <td class="contents-width"><%= room.created_at.to_s(:datetime_jp) %></td>
  </tr>
  <br>
  <% end %>
  </table>
  

  </div>
</div>